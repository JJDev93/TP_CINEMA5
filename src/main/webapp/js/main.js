$('.chartSlide').slick({
    arrows:true,
    infinite: true,
    slidesToShow: 5,
    slidesToScroll: 1,
    prevArrow: $('#slide_visual_prev'),
    nextArrow: $('#slide_visual_next')
});
$('.eventList').slick({
    infinite: true,
    slidesToShow: 3,
    slidesToScroll: 1
});